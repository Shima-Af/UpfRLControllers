# cluster_series.npy — service=Netflix, K=10, from UpfTrafficForecaster
# @ feature/cluster-first-stgnn (data/cluster_first/Netflix/K10/).
#
# The md5 below is the CANONICAL pin — the blob lives in the `forecaster`
# S3 remote and `dvc pull` fetches it by hash. The earlier comment citing
# 'commit 020373a' was stale: train/val prediction arrays were added
# upstream in LATER commits (04ed1c1, ff90c8a), so no single source commit
# describes the current external set. TODO: once UpfTrafficForecaster
# declares these as DVC pipeline outputs, replace this hand-written pointer
# with a real `dvc import` (repo+path+rev dep).
outs:
- md5: d80395e995e372ce3fc9fc9ca473f03c
  hash: md5
  path: cluster_series.npy
  remote: forecaster
